import 'package:flutter/material.dart';

// A utility class to define custom colors for the app theme
class CustomColors {
  // Primary color used throughout the app for general UI elements
  static const Color primary = Color(0xFFB09E99);

  // Secondary color, typically used for complementary accents or backgrounds
  static const Color secondary = Color(0xFF7FD1B9);

  // Accent color, used to highlight important components like buttons or indicators
  static const Color accent = Color(0xFFF6AE2D);

  // Dark accent color, often used for headers or focus areas
  static const Color dark_accent = Color(0xFF227c9d);

  static const Color orangeAccent = Color(0xFFEE570B);
}
